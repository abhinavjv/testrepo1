def apply_flat_discount(total_amount, discount_val):
    # BUG: SYNTAX error - missing colon
    if total_amount > 100
        # BUG: LOGIC error - adding discount instead of subtracting
        return total_amount + discount_val 
    return total_amount